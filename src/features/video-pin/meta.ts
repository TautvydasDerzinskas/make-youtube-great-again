import IMeta from '../../interfaces/meta';

const meta: IMeta = {
  id: 'myga-video-pin',
  description: 'Allows you to watch video while reading comments',
  title: 'Pin video on scroll',
  defaultStatus: false,
  videoPageOnly: true,
  isInHistoryTab: false,
};

export default meta;
