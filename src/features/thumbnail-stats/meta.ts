import IMeta from '../../interfaces/meta';

const meta: IMeta = {
  id: 'myga-thumb-preview',
  description: 'Displays like/dislike statistics on video thumbnail on mouse hover',
  title: 'Thumbnail stats'
};

export default meta;
